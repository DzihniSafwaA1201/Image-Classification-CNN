# Image-Classification-CNN
Simple CNN implementation for bean leaf disease classification using the Bean Disease Dataset. The model was built with TensorFlow/Keras using convolution layers, ReLU, pooling, and fully connected layers. Evaluation was performed using accuracy, loss curves, and confusion matrices.
